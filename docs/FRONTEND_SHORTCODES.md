# نمایش frontend

برای نمایش رابط کاربر در یک برگه WordPress:

```text
[panje_diet_form]
```

کاربر باید وارد حساب WordPress باشد. برای تاریخچه گزارش‌ها در یک برگه جدا:

```text
[panje_user_history]
```
